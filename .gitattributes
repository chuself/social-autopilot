# Shell scripts run on Linux runners. Checked out with CRLF they fail with an
# unreadable "\r: command not found", so pin them to LF regardless of platform.
*.sh text eol=lf
*.mjs text eol=lf
*.js text eol=lf
*.yml text eol=lf
